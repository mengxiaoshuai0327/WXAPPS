<!--pages/messages/list/list.wxml-->
<view class="container">
  <view class="header">
    <view class="header-title">系统消息</view>
  </view>

  <view class="messages-list" wx:if="{{messages.length > 0}}">
    <view class="message-item" wx:for="{{messages}}" wx:key="id" bindtap="viewMessageDetail" data-id="{{item.id}}">
      <view class="message-icon">🔔</view>
      <view class="message-content">
        <view class="message-title">{{item.title}}</view>
        <view class="message-time">{{item.created_at_formatted}}</view>
      </view>
      <view class="message-arrow">></view>
    </view>
  </view>

  <view wx:if="{{messages.length === 0 && !loading}}" class="empty-state">
    <text>暂无系统消息</text>
  </view>

  <view wx:if="{{loading}}" class="loading">加载中...</view>
</view>

